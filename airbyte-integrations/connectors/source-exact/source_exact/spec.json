{
  "documentationUrl": "https://docsurl.com",
  "connectionSpecification": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Exact Spec",
    "type": "object",
    "required": ["client_key", "secret", "authorization_code"],
    "additionalProperties": false,
    "properties": {
      "client_id": {
        "type": "string",
        "description": "Exact API OAuth2 client key"
      },
      "client_secret": {
        "type": "string",
        "description": "Exact API OAuth2 secret"
      },
      "authorization_code": {
        "type": "string",
        "description": "One-time authorization token"
      }
    }
  }
}
